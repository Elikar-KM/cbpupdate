'use client'

// Next Imports

const LangRedirect = () => {
  // La redirection via code langue est désactivée car l'application utilise désormais des URL à la racine
  return null
}

export default LangRedirect
